# Disable MCP server authentication.
#
# Only use this when the server is not reachable by untrusted clients, such as
# an HTTP listener bound to the loopback interface.

[server]
host = 127.0.0.1
port = 8000

[auth]
method = none
